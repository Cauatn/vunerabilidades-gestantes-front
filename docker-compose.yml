services:
  frontend:
    build:
      context: .
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-/api}
    image: gestare-frontend
    ports:
      - "${FRONTEND_PORT:-8080}:3000"
    restart: unless-stopped
